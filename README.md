# WA-AI Frontend

React dashboard for the WhatsApp AI assistant.

```bash
# from repo root (recommended)
npm run setup
npm run dev
```

Or this folder only (API must already be running on :3003):

```bash
npm install
npm start
```

App: **http://localhost:3002**

Copy `.env.example` to `.env` if you do not already have one.
